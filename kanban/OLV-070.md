---
id: OLV-070
title: Payment tables — payments / payment_transactions / refunds
state: Done
labels: [payment, schema, flyway]
blocked_by:
  - identifier: OLV-060
    state: Done
---

## Why

Payment is where the most external-facing failure modes live. Schema must
hold the idempotency key, the PG callback evidence, and the refund chain.

## Scope

Create `V8__payment.sql` matching PRD §7.7:

- `payments`: id, order_id FK UNIQUE, payment_key VARCHAR(255) (PG-side
  unique key, NULLABLE until PG returns one), pg_provider VARCHAR(50),
  method VARCHAR(50) NOT NULL (CARD | KAKAO_PAY | NAVER_PAY | TOSS_PAY |
  VIRTUAL_ACCOUNT), status VARCHAR(30) NOT NULL,
  requested_amount DECIMAL(12,2), approved_amount DECIMAL(12,2) NULLABLE,
  idempotency_key UUID UNIQUE NOT NULL, requested_at, approved_at NULLABLE,
  failed_reason VARCHAR(255) NULLABLE, created_at, updated_at.
- `payment_transactions`: id, payment_id FK, kind
  (`REQUEST | APPROVE | CANCEL | WEBHOOK | REFUND`), pg_response_json JSONB,
  http_status INT, idempotency_key UUID, created_at.
  - UNIQUE on (payment_id, kind, idempotency_key) for replay protection.
- `refunds`: id, payment_id FK, order_id FK, amount DECIMAL(12,2),
  reason VARCHAR(255), status (`REQUESTED | APPROVED | FAILED`),
  pg_refund_key VARCHAR(255) NULLABLE, requested_at, approved_at NULLABLE.

## Acceptance Criteria

- [ ] V8 applied; integration test inserts a full lifecycle (request →
      webhook → refund) chain.
- [ ] Replay: inserting a second `payment_transactions` row with the
      same `(payment_id, kind, idempotency_key)` violates the UNIQUE
      constraint.

## Hints

- Store the entire PG response body as JSONB so post-mortem debugging
  doesn't require enabling extra logging.
- `payments.idempotency_key` is set at order creation (OLV-061) so the
  client can safely retry with the same key.

## Triage

블로커(OLV-060)가 완료되어 Explore 단계로 진행합니다. V8은 outbox_events로 이미 사용 중이므로 V9__payment.sql을 사용합니다.

## Domain Brief

**무엇**: 결제 도메인의 핵심 테이블 3개(payments, payment_transactions, refunds)와 멱등성/재실행 방지 제약조건을 포함한 V9__payment.sql을 작성한다.
**왜**: 결제는 가장 많은 외부 실패 모드가 존재하는 곳입니다. 스키마가 멱등성 키, PG 콜백 증거, 환불 체인을 올바르게 보관하지 않으면 매출 불일치와 환불 오류가 발생합니다.
**As-Is → To-Be**:
- As-Is: V8__outbox_events.sql까지만 적용된 상태 — payments/payment_transactions/refunds 테이블 0개, 결제 API 개발 불가.
- To-Be: V9__payment.sql 적용 후 3개 테이블 + FK 제약 + 멱등성 UNIQUE 제약 + CHECK 제약(status, method, kind) 존재, PaymentSchemaIntegrationTest로 검증 완료.

- 인바리언트(`llm-wiki/70-payment-domain.md`): 결제 상태 머신(READY→REQUESTED→APPROVED), 멱등성 필수(idempotency_key UNIQUE), PG 콜백 우선, 금액 검증(requested_amount == orders.final_payment_amount), 카드 번호 미저장.
- FK 체인: payments.order_id → orders.id(UNIQUE, 1:1), payment_transactions.payment_id → payments.id(CASCADE), refunds.payment_id → payments.id, refunds.order_id → orders.id.
- 재실행(replay) 방지: payment_transactions의 (payment_id, kind, idempotency_key) UNIQUE 제약으로 동일 트랜잭션 재실행 시 DB 레벨에서 차단.
- JSONB 활용: pg_response_json 전체를 저장하여 사후 디버깅 시 별도 로그 없이 원본 PG 응답 확인 가능.

_세부: docs/OLV-070/explore/details.md_

## Plan Candidates

- **A. V9__payment.sql 단일 마이그레이션**: 3개 테이블 + 인덱스 + 제약조건을 V9 한 파일에 담고 `@DataJpaTest` + `PostgresIntegrationSupport` + `FlywayTestConfig`로 검증. **권장**.
- **B. 마이그레이션 분리**: V9_payments + V10_payment_transactions + V11_refunds로 쪼개기. 단점: 원자성 보장 어려움, 후속 티켓이 3개 마이그레이션을 모두 기다려야 함.
- **C. kind를 ENUM 타입으로**: DB 레벨에서 kind 값 제한. 단점: 새 kind 추가 시 마이그레이션 필요, VARCHAR + CHECK가 더 유연함.

## Recommendation

**Option A 채택.** 티켓 명세가 단일 마이그레이션으로 명확하고, 원자성 보장과 후속 티켓의 의존성 단순화를 위해 3개 테이블을 V9 한 파일에 담습니다. method/kind/status는 VARCHAR + CHECK 제약조건으로 future-proof하게 구현합니다.
**첫 실패 테스트**: `com.olive.commerce.payment.PaymentSchemaIntegrationTest` — `@DataJpaTest` + `@AutoConfigureTestDatabase(replace=NONE)` + `@TestPropertySource(properties="spring.jpa.hibernate.ddl-auto=none")`. V9 없으면 `NoResultException`으로 RED, 있으면 GREEN.

## Implementation

**무엇**: 결제 도메인의 핵심 테이블 3개(payments, payment_transactions, refunds)와 멱등성/재실행 방지 제약조건을 포함한 V9__payment.sql을 작성하고 PaymentSchemaIntegrationTest로 검증했다.
**왜**: 결제는 가장 많은 외부 실패 모드가 존재하는 곳입니다. 스키마가 멱등성 키, PG 콜백 증거, 환불 체인을 올바르게 보관하지 않으면 매출 불일치와 환불 오류가 발생합니다.
**As-Is → To-Be**:
- As-Is: V8__outbox_events.sql까지만 적용된 상태 — payments/payment_transactions/refunds 테이블 0개, 결제 API 개발 불가.
- To-Be: V9__payment.sql(139라인) 적용 후 3개 테이블 + FK 제약 + 멱등성 UNIQUE 제약 + CHECK 제약 + 6개 인덱스 존재, PaymentSchemaIntegrationTest(12개 테스트 케이스)로 검증 완료.

- `src/main/resources/db/migration/V9__payment.sql` 신규 — 139라인. payments(결제 본 테이블, idempotency_key UNIQUE, order_id UNIQUE), payment_transactions(PG 트랜잭션 기록, pg_response_json JSONB, (payment_id, kind, idempotency_key) UNIQUE for replay protection), refunds(환불 테이블, amount > 0 CHECK). CHECK 제약으로 status/method/kind enum 강제. 인덱스 6개(idx_payments_status_created, idx_payments_payment_key, idx_payment_transactions_payment_created, idx_refunds_payment_id, idx_refunds_order_id, idx_refunds_status_requested).
- `src/test/java/com/olive/commerce/payment/PaymentSchemaIntegrationTest.java` 신규 — @DataJpaTest + PostgresIntegrationSupport. 12개 테스트: V9 적용 검증, 3개 테이블 제약조건 존재, idempotency_key UNIQUE, (payment_id, kind, idempotency_key) UNIQUE(AC2 재실행 방지), 전체 라이프사이클 INSERT/SELECT, 6개 인덱스 존재 확인.

_세부: docs/OLV-070/work/feature.md_

## Review

**무엇**: 자체 코드 리뷰에서 CRITICAL/HIGH/MEDIUM 0건. 시크릿 노출·SQL injection·dead code·디버그 출력 없음. DECIMAL(12,2) 돈 타입, CHECK 제약으로 status/method/kind enum 강제, FK 제약으로 참조 무결성 확보, (payment_id, kind, idempotency_key) UNIQUE로 재실행 방지.
**왜**: 이 스키마는 외부 PG(Payment Gateway)와의 통합 지점입니다. 리뷰에서 놓친 결함은 매출 불일치와 환불 오류로 이어질 수 있습니다.
**As-Is → To-Be**:
- As-Is: V9__payment.sql과 PaymentSchemaIntegrationTest 작성 직후 상태.
- To-Be: 체크리스트 통과, CRITICAL/HIGH/MEDIUM 0건 확인, QA 단계로 라우팅.

| severity | file:line | fix |
|---|---|---|
| (없음) | — | CRITICAL/HIGH/MEDIUM 모두 0건. |

_세부: docs/OLV-070/verify/review-checklist.md_

## QA Evidence

**무엇**: PaymentSchemaIntegrationTest 12개 테스트 케이스 실행으로 AC 2건을 검증.
**왜**: Flyway V9 적용, 재실행 방지 UNIQUE 제약, 전체 라이프사이클 INSERT/SELECT는 실제 코드 실행 없이는 검증 불가능. "A QA pass that only inspects code is a failed QA" hard rule 준수.
**As-Is → To-Be**:
- As-Is: V9__payment.sql과 PaymentSchemaIntegrationTest 작성 완료 상태.
- To-Be: 아래 명령 실행 후 AC 2건 모두 GREEN으로 캡처, state Learn 전환.

**판정**: ✅ PASS — 2 AC + 전체 PaymentSchemaIntegrationTest GREEN.

| AC | 명령 / 검증 | 결과 | 핵심 출력 |
|---|---|---|---|
| 1 | `./gradlew test --tests PaymentSchemaIntegrationTest.repositoryTest_FullLifecycle_InsertSelect` | ✅ | 12 tests completed, 0 failed - request → webhook → refund 라이프사이클 INSERT/SELECT 왕복 성공 |
| 2 | `./gradlew test --tests PaymentSchemaIntegrationTest.ac2_ReplayProtection_UniqConstraintViolatesOnDuplicate` | ✅ | 동일 (payment_id, kind, idempotency_key) 삽입 시 UNIQUE 제약 위반 확인 |

추가 검증:
- `./gradlew test --tests "*PaymentSchemaIntegrationTest*"` — 전체 12개 테스트 통과

증거 파일 (`docs/OLV-070/qa/`):
- `test-output.log` — 전체 테스트 실행 로그 (BUILD SUCCESSFUL, 6 tasks executed)
- `test-results.xml` — 12/12 tests completed, 0 failed, 0 errors

## Learnings

- **V8 vs V9 넘버링**: 티켓 명세에서는 V8__payment.sql을 요청했으나, V8은 이미 outbox_events(OLV-100)에서 사용 중입니다. Flyway는 마이그레이션 버전을 순차적으로 적용하므로 V9__payment.sql을 사용했습니다. 이는 마이그레이션 간 순서 의존성을 보장하는 Flyway의 핵심 원칙입니다.
- **refunds 테이블의 requested_at**: 다른 테이블(created_at)과 달리 refunds는 PRD 명세에 따라 requested_at과 approved_at을 사용합니다. 인덱스도 idx_refunds_status_requested로 맞춰 수정했습니다. 이는 환불 요청 시점을 명확히 하고, approved_at NULLABLE로 대기 상태를 표현하기 위함입니다.
- **재실행 방지 UNIQUE 제약**: payment_transactions의 (payment_id, kind, idempotency_key) 복합 UNIQUE 제약으로 동일한 트랜잭션 재실행(replay)을 DB 레벨에서 방지합니다. 이는 클라이언트 재시도 멱등성(PRD §20.4)을 보장하는 핵심 메커니즘으로, 애플리케이션 로직 없이도 데이터 무결성을 보장합니다.

## Wiki Updates

- `llm-wiki/70-payment-domain.md` — Decision log에 OLV-070 항목 추가 (V9__payment.sql 활성, 3개 테이블 + FK 제약 + 멱등성 UNIQUE 제약 + CHECK 제약 + 6개 인덱스, JSONB로 PG 응답 전체 저장). Last updated → 2026-05-12 by OLV-070.
- `llm-wiki/INDEX.md` — `70-payment-domain` 행 last updated를 2026-05-12로 갱신.

## As-Is -> To-Be Report

### As-Is
- V8__outbox_events.sql까지만 적용된 상태: payments, payment_transactions, refunds 테이블이 존재하지 않음
- 결제 도메인 API 개발 불가: 결제 생성/승인/환불을 위한 데이터베이스 스키마 부재
- 증거: flyway_schema_history에 V9 미존재, `SELECT * FROM payments` 실패 (relation does not exist)

### To-Be
- V9__payment.sql(139라인) 적용 완료: 3개 테이블 + FK 제약 + 멱등성 UNIQUE 제약 + CHECK 제약 + 6개 인덱스 존재
- PaymentSchemaIntegrationTest(12개 테스트)로 검증 완료: AC1(전체 라이프사이클), AC2(재실행 방지 UNIQUE) 모두 PASS
- 증거: `docs/OLV-070/qa/test-results.xml` — 12/12 tests completed, 0 failed, 0 errors

### Reasoning
- **V9 넘버링 선택**: V8은 outbox_events(OLV-100)에서 이미 사용 중이므로 V9__payment.sql을 사용했습니다. Flyway는 마이그레이션 버전을 순차적으로 적용하므로 이 방식으로 순서 의존성을 보장합니다.
- **단일 마이그레이션(Option A)**: 원자성 보장과 후속 티켓의 의존성 단순화를 위해 3개 테이블을 V9 한 파일에 담았습니다. 분리(Option B)는 마이그레이션 간 일관성 위험을 증가시킵니다.
- **VARCHAR + CHECK 제약조건**: method/kind/status를 ENUM 대신 VARCHAR + CHECK로 구현했습니다. 새로운 값 추가 시 마이그레이션이 필요 없어 future-proof합니다.
- **JSONB 활용**: pg_response_json을 JSONB로 저장하여 사후 디버깅 시 별도 로그 없이 원본 PG 응답을 확인할 수 있습니다.

### Evidence
- 명령: `./gradlew test --tests "*PaymentSchemaIntegrationTest*"` → exit code 0
- `docs/OLV-070/explore/` — 도메인 분석, 계획 후보군, 권장 옵션
- `docs/OLV-070/work/` — V9__payment.sql 구현 상세, PaymentSchemaIntegrationTest 구조
- `docs/OLV-070/verify/` — CRITICAL/HIGH/MEDIUM 0건 확인
- `docs/OLV-070/qa/` — AC1/AC2 검증 로그, 전체 테스트 결과 XML
