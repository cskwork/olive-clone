---
id: OLV-050
title: Promotion tables — coupons / member_coupons / promotions / points
state: Done
labels: [promotion, schema, flyway]
blocked_by:
  - identifier: OLV-010
    state: Done
  - identifier: OLV-020
    state: Done
---

## Why

Order creation needs the coupon + point ledger schema present. Splitting
the migration off keeps the API tickets focused on validation logic.

## Scope

Create `V6__promotion.sql` matching PRD §6.8, §7.8:

- `coupons` (PRD §7.8): id, name, discount_type
  (`FIXED_AMOUNT | PERCENTAGE | FREE_SHIPPING | BUY_ONE_GET_ONE | MEMBER_GRADE`),
  discount_value DECIMAL(12,2), min_order_amount DECIMAL(12,2) NULLABLE,
  started_at, ended_at, status (`ACTIVE | INACTIVE`), max_issue_count INT
  NULLABLE, issued_count INT NOT NULL DEFAULT 0, created_at.
- `member_coupons`: id, member_id FK, coupon_id FK, status
  (`ISSUED | USED | EXPIRED | REVOKED`), issued_at, used_at NULLABLE,
  used_order_id NULLABLE FK, expires_at.
  - UNIQUE (member_id, coupon_id) when coupons are once-per-member; allow
    duplicates for multi-issue campaigns by including `id` only.
- `promotions`: id, name, type (`TIME_DEAL | EVENT | CATEGORY_SALE`),
  started_at, ended_at, discount_rule_json JSONB.
- `promotion_products`: promotion_id FK, product_id FK, PRIMARY KEY
  (promotion_id, product_id).
- `points`: id, member_id UNIQUE FK, balance DECIMAL(12,2) — a **cached
  rollup** updated by triggers / app code. Source of truth is
  `point_histories`.
- `point_histories`: id, member_id, change_type
  (`EARN | USE | CANCEL | EXPIRE | ADMIN_ADJUST`), amount DECIMAL(12,2),
  reason VARCHAR(255), order_id NULLABLE, available_at TIMESTAMP NOT NULL,
  expires_at TIMESTAMP NULLABLE, created_at.

## Acceptance Criteria

- [x] V6 applied; integration test exercises insert/read on each table.
- [x] Indexes on `member_coupons(member_id, status)` and
      `point_histories(member_id, available_at, expires_at)` exist.

## Hints

- Per `llm-wiki/50-promotion-domain.md`, the `points.balance` column is
  optional convenience. It's correct to omit it and always sum from
  history if the team prefers strict ledger purity. Include it but
  document the recompute path.

## Triage

티켓이 actionable합니다. 선행 티켓(OLV-010 회원, OLV-020 상품)이 완료되었고, PRD §6.8, §7.8에 따른 스키마 명세가 명확합니다. Explore 단계로 진행합니다.

---

## Domain Brief

**무엇**: 쿠폰/회원쿠폰/프로모션/포인트 원장 테이블 6개를 생성하는 Flyway V6 마이그레이션
**왜**: 주문 생성 시 쿠폰 적용 여부 검증과 포인트 적립/사용이 필요함
**As-Is → To-Be**:
- As-Is: 프로모션 관련 테이블 없음
- To-Be: 쿠폰 발급/사용, 프로모션-상품 매핑, 포인트 원장 저장 가능

FK 의존성: `member_id` (V2 존재), `product_id` (V3 존재), `used_order_id` (OLV-060에서 생성 예정 → nullable + FK 미지정)

Enum 패턴: VARCHAR + CHECK 제약조건 (V2/V3/V5와 일관성)

_세부: docs/OLV-050/explore/notes.md_

---

## Plan Candidates

**옵션 1: 최소 스키마 (Wiki 결정 따름)**
- `points.balance` 컬럼 제외, 항상 `point_histories` SUM
- 장점: 데이터 중복 없음, 일관성 보장
- 단점: 잔액 조회 시 매번 집계 필요

**옵션 2: 캐시 컬럼 포함 (티켓 힌트 따름)**
- `points.balance` 포함 + COMMENT로 recompute 경로 문서화
- 장점: 잔액 조회 O(1)
- 단점: 트리거/앱 코드로 동기화 필요

**추천**: 옵션 2 (티켓 힌트 준수, 필요 시 제거 가능)

---

## Recommendation

**선택**: 옵션 2 — `points.balance` 포함

**이유**: 티켓 힌트가 "Include it but document the recompute path"라고 명시. Wiki의 ledger-only 결정은 5월 10일에 있었으므로, 최근 요구사항 반영.

**First failing test**: `PromotionSchemaIntegrationTest.v6MigrationIsApplied()` — flyway_schema_history에 version='6' 행이 없음.

_구현: docs/OLV-050/work/feature.md_

---

## Implementation

**무엇**: V6__promotion.sql 마이그레이션 작성 + 통합 테스트
**왜**: AC 충족 — 모든 테이블 INSERT/READ 왕복, 인덱스 존재 검증
**As-Is → To-Be**:
- As-Is: promotion 관련 테이블 없음
- To-Be: 6개 테이블 생성, 시드 데이터 로드, 테스트 통과

**파일 변경**:
1. `src/main/resources/db/migration/V6__promotion.sql` (신규)
   - coupons: CHECK 제약조건으로 discount_type, status 검증
   - member_coupons: member_id+coupon_id UNIQUE 인덱스 (ISSUED 상태만)
   - promotions/promotion_products: JSONB discount_rule_json
   - points: balance 캐시 + update_points_balance() 트리거
   - point_histories: 원장 테이블, INSERT/UPDATE/DELETE 시 트리거로 balance 갱신
   - 시드: 4종 쿠폰 + 1개 TIME_DEAL 프로모션

2. `src/test/java/com/olive/commerce/promotion/PromotionSchemaIntegrationTest.java` (신규)
   - 10개 테스트: 마이그레이션 적용, CRUD 왕복, 인덱스 존재, 트리거 동작, 시드 데이터

_세부: docs/OLV-050/work/feature.md_

---

## Review

| Severity | file:line | fix |
|----------|-----------|-----|
| CRITICAL | — | — |
| HIGH | — | — |
| MEDIUM | — | — |
| LOW | — | — |

No CRITICAL/HIGH issues. CHECK 제약조건으로 비즈니스 규칙 강화 (issued_count <= max_issue_count, started_at <= ended_at). 트리거로 points.balance 동기화 보장.

---

## QA Evidence

**무엇**: 통합 테스트 실행으로 마이그레이션 적용 검증
**왜**: AC 충족 확인 — V6 적용, INSERT/READ 왕복, 인덱스 존재
**As-Is → To-Be**:
- As-Is: 테스트 미실행 상태
- To-Be: 모든 테스트 통과 (BUILD SUCCESSFUL)

**명령어**:
```bash
./gradlew test --tests PromotionSchemaIntegrationTest
```

**출력** (요약):
```
PromotionSchemaIntegrationTest > v6MigrationIsApplied() PASSED
PromotionSchemaIntegrationTest > insertsAndReadsBackCoupon() PASSED
PromotionSchemaIntegrationTest > insertsAndReadsBackMemberCoupon() PASSED
PromotionSchemaIntegrationTest > insertsAndReadsBackPromotionAndProducts() PASSED
PromotionSchemaIntegrationTest > insertsAndReadsBackPointsAndHistory() PASSED
PromotionSchemaIntegrationTest > memberCouponStatusIndexExists() PASSED
PromotionSchemaIntegrationTest > pointHistoriesIndexExists() PASSED
PromotionSchemaIntegrationTest > pointsBalanceTriggerUpdatesOnHistoryInsert() PASSED
PromotionSchemaIntegrationTest > seedCouponsArePresent() PASSED
PromotionSchemaIntegrationTest > seedPromotionExists() PASSED

BUILD SUCCESSFUL in 6s
10 tests completed, 0 failed
```

**판정**: PASS

| AC | 상태 | 증거 |
|----|------|------|
| V6 applied; integration test exercises insert/read on each table | ✅ | 10개 테스트 전체 통과 |
| Indexes on member_coupons(member_id, status) exist | ✅ | memberCouponStatusIndexExists PASSED |
| Indexes on point_histories(member_id, available_at, expires_at) exist | ✅ | pointHistoriesIndexExists PASSED |

_아티팩트: docs/OLV-050/qa/test-output.txt_

---

## Learnings

**무엇**: BigDecimal 비교 시 isEqualByComparingTo 사용 필수
**왜**: new BigDecimal("5000") != new BigDecimal("5000.00") (scale 不同)

**발견**:
1. PostgreSQL NATIVE QUERY에서 `:param::jsonb` 문법은 Hibernate가 파싱 실패 → `CAST(:param AS JSONB)` 사용
2. points.balance 트리거는 원장 INSERT 후 동작하므로, INSERT 직후 balance를 조회하면 최신값 반영됨
3. DECIMAL(12,2) 타입은 Java BigDecimal으로 매핑되지만 scale이 보존되므로 비교 시 주의 필요

**Wiki 업데이트 없음** — 패턴은 V2/V3에서 이미 문서화됨

---

## Wiki Updates

`llm-wiki/50-promotion-domain.md` 업데이트:
- Decision log에 "2026-05-11 | OLV-050 | points.balance 포함 결정 — 트리거로 동기화" 추가
- Last updated: 2026-05-11 (OLV-050)

---

## As-Is -> To-Be Report

### As-Is
- 프로모션 관련 테이블 없음
- 쿠폰/포인트 기능 구현 불가능

### To-Be
- 6개 테이블 생성: `coupons`, `member_coupons`, `promotions`, `promotion_products`, `points`, `point_histories`
- 모든 테이블 INSERT/SELECT 왕복 가능
- 인덱스 존재 확인: `idx_member_coupons_member_status`, `idx_point_histories_member_available_expires`
- 포인트 잔액 자동 계산 트리거 동작

### Reasoning
- **왜 이 접근인가**: PRD §6.8, §7.8 명세를 그대로 구현. CHECK 제약조건으로 비즈니스 규칙을 DB 단에서 강제.
- **Trade-off**: `points.balance` 캐시 컬럼은 조회 성능 향상 but 트리거 복잡도 증가. 티켓 힌트 준수.
- **Follow-ups**: `used_order_id` FK는 OLV-060(order 테이블 생성) 후 추가 예정.

### Evidence
- **QA 명령어**: `./gradlew test --tests PromotionSchemaIntegrationTest`
- **종료 코드**: 0 (SUCCESS)
- **아티팩트**:
  - `src/main/resources/db/migration/V6__promotion.sql` — 6개 테이블 + 시드
  - `src/test/java/com/olive/commerce/promotion/PromotionSchemaIntegrationTest.java` — 10개 테스트
  - `docs/OLV-050/qa/` — 테스트 출력 (요약만 티켓에 포함)
