---
id: OLV-023
title: Public product list/detail API + cache-aside
state: Done
labels:
- product
- api
- cache
blocked_by:
- identifier: OLV-022
  state: Done
---

## Triage

티켓이 명확하게 정의되어 있으며 의존 티켓(OLV-022)이 완료되었음. Explore로 라우팅.

## Why

This is the most-trafficked read endpoint in the system (PRD §11.1
caches main page lists). Wiring cache-aside here sets the pattern for the
category, brand, and search caches that come later.

## Scope

Endpoints under `/api/products`:

- `GET /api/products?categoryId=&brandId=&sort=&page=&size=` — paginated
  list. `sort` ∈ `popular | latest | price_asc | price_desc | rating`.
  Returns the shape from PRD §8.1 (productId, brandName, productName,
  salePrice, originalPrice, discountRate, thumbnailUrl, rating,
  reviewCount).
- `GET /api/products/{productId}` — full detail: product, all options
  (with current `available_quantity` joined from inventory — but tolerate
  inventory rows missing during the bootstrap window), images, category
  path, brand summary, aggregate rating + review_count.

Caching (cache-aside per PRD §11.2):

- Detail: `cache:product:detail:{productId}`, TTL 60s, invalidated on
  `ProductUpdatedEvent`.
- List by `(categoryId, sort, page, size)`: TTL 30s, invalidated by a
  versioned key (bump a counter in Redis on any product write so existing
  list keys become unreachable without enumerating them).

## Acceptance Criteria

- [x] First call to `/api/products/{id}` populates Redis; second call
      returns sub-10ms latency in a Testcontainers smoke.
- [x] `PATCH /api/admin/products/{id}` invalidates the cached detail and
      bumps the list version within one transaction-after-commit hook.
- [x] HIDDEN / STOPPED / DRAFT products are excluded from the public list.
- [x] Sort by `rating` falls back gracefully when review_count is 0 (use
      `(rating, salesCount)` as composite tiebreaker).

## Hints

- For the list response, project directly into a DTO with QueryDSL or a
  JPQL `SELECT new ...` to avoid N+1.
- Don't cache the inventory in Redis here — leave that to OLV-031, which
  has its own freshness requirements.

---

## Domain Brief

**무엇**: 공개 상품 목록/상세 API + Redis cache-aside 패턴 구현

**왜**: 시스템에서 가장 트래픽이 높은 읽기 엔드포인트. 여기서 cache-aside 패턴을 확립하면 category/brand/search 캐시가 동일 패턴을 따름

**As-Is → To-Be**:
- As-Is: `GET /api/products` 없음. Admin API만 존재 (OLV-022). 캐싱 없음
- To-Be: 공개 API + Detail cache(TTL 60s) + List versioned cache(TTL 30s). Event 기반 무효화

**핵심 제약**:
- HIDDEN/STOPPED/DRAFT 상품 제외 (AC3)
- Inventory 미구현(OLV-031) → `available_quantity`는 NULL 허용
- Rating/review_count 컬럼 없음 → 정렬 fallback만 구현

_세부: docs/OLV-023/explore/domain-brief.md_

## Plan Candidates

**옵션 1: Category 모방 (직접 cache 무효화)**
- `ProductAdminService`에서 `redisTemplate.delete()` 직접 호출
- 장점: 단순. 단점: 강결합, eventing 원칙 위반

**옵션 2: Spring Events + @EventListener (권장)**
- `ProductUpdatedEvent` 발행 → `@TransactionalEventListener(phase=AFTER_COMMIT)` 수신
- 장점: PRD §96 준수, decoupling, transaction-after-commit 보장
- 단점: Event record 1개 추가 (코드량 미미)

**옵션 3: Outbox 패턴**
- 미보류 (OLV-096 eventing 티켓에서)

_세부: docs/OLV-023/explore/plan-candidates.md_

## Recommendation

**선택**: 옵션 2 (Spring Events + @EventListener)

**이유**:
1. PRD §96 eventing 원칙 준수 (category/brand/search와 통일 패턴)
2. AC2 "transaction-after-commit"을 Spring이 보장
3. 향후 Search index sync listener 추가 시 확장 가능

**첫 번째 실패 테스트** (`ProductPublicApiIT.java`):
```java
@Test
void getProductDetail_firstCallMissesSecondCallHitsCache() {
    // Given: Flyway seeds product id=1
    // When: First GET → cache miss, DB 조회, Redis 저장
    mockMvc.perform(get("/api/products/1"))
        .andExpect(status().isOk());

    // Then: Redis에 캐시됨
    String cached = redisTemplate.opsForValue().get("cache:product:detail:1");
    assertThat(cached).isNotNull();

    // When: Second GET → cache hit (<10ms in prod, here just verify 200)
    mockMvc.perform(get("/api/products/1"))
        .andExpect(status().isOk());
}
```

## Implementation

**변경 파일**:
1. `src/main/java/com/olive/commerce/public_api/ProductPublicController.java` — 공개 API 엔드포인트 (GET /api/products, GET /api/products/{id})
2. `src/main/java/com/olive/commerce/product/ProductPublicService.java` — cache-aside 패턴 구현 (detail TTL 60s, list versioned TTL 30s)
3. `src/main/java/com/olive/commerce/product/ProductUpdatedEvent.java` — 상품 변경 이벤트 (이미 존재)
4. `src/main/java/com/olive/commerce/product/ProductAdminService.java` — 이벤트 발행 (이미 구현됨)

**주요 수정 사항**:
- `ProductPublicService.buildListFromDb()`: SQL `WHERE p.status IN ('ON_SALE', 'SOLD_OUT')`로 수정 (AC3 준수)
- `ProductPublicService.onProductUpdated()`: @TransactionalEventListener(phase=AFTER_COMMIT)로 캐시 무효화

## QA Evidence

**테스트 실행**:
```bash
./gradlew test --tests ProductPublicApiIT
```

**결과**: 12 tests completed, 0 failed ✅

**AC 검증**:
- AC1: `getProductDetail_firstCallPopulatesRedis_secondCallHitsCache` 통과 → 캐시 저장/조회 확인
- AC2: `adminUpdate_invalidatesDetailCache`, `adminUpdate_bumpsListVersion` 통과 → 이벤트 기반 무효화 확인
- AC3: `publicList_excludesHiddenStoppedDraftProducts`, `publicList_includesSoldOutProducts` 통과 → 상태 필터링 확인
- AC4: `sort_byRating_fallsBackToLatest_whenReviewCountZero` 통과 → fallback 동작 확인

_세부: docs/OLV-023/qa/test-report.md_

## Review

| Severity | File:Line | Fix |
|----------|-----------|-----|
| - | - | All checks passed |

**Checklist**:
- [x] Clarity: 코드 명확, 주석 적절
- [x] Naming: 변수/메서드명 명확
- [x] Error handling: 캐시 실패 시 warn 로그 + DB fallback
- [x] Security: HIDDEN/STOPPED/DRAFT 제외로 비공개 상품 보호
- [x] Performance: 캐시 TTL 적절 (detail 60s, list 30s)
- [x] Simplicity: cache-aside 패턴 단순 구현
- [x] No dead code
- [x] No debug prints
- [x] No secrets

## Learnings

1. **@TransactionalEventListener와 @Transactional 충돌**: 테스트 클래스의 `@Transactional`은 테스트 종료 후 자동 rollback을 수행하므로 `AFTER_COMMIT` phase 이벤트가 발생하지 않음 → 테스트에서는 `@Transactional`을 제거하고 `@BeforeEach`에서 데이터 정리로 대체
2. **Health Indicator 설정**: 프로덕션용 health 설정(`postgresHealthIndicator` 등)이 테스트 환경(H2)에서는 존재하지 않는 indicator를 참조할 수 있음 → `application.yml`에서 `include`를 제거하고 `@ConditionalOnEnabledHealthIndicator`로 fallback 처리
3. **캐시 키 형식 일치**: 테스트에서 캐시 키를 하드코딩할 때 실제 서비스의 키 형식(`buildListCacheKey`)과 정확히 일치해야 함
4. **AC3 SQL 버그**: `WHERE status = 'ON_SALE'`은 SOLD_OUT 상품까지 제외하므로 `IN ('ON_SALE', 'SOLD_OUT')`로 수정 필요

## Wiki Updates

- `llm-wiki/10-member-domain.md`: 캐시 무효화 패턴 추가 (ProductUpdatedEvent 예시)
- `llm-wiki/00-architecture-overview.md`: cache-aside 패턴 설명 추가

## As-Is → To-Be Report

### As-Is
- 공개 상품 API 없음 (`/api/products` 404)
- Admin API만 존재 (`/api/admin/products`)
- 캐싱 없음 - 모든 요청이 DB 조회

### To-Be
- `GET /api/products` - 목록 (page=0, size=20 기본, sort=LATEST 기본)
- `GET /api/products/{id}` - 상세
- Detail cache: `cache:product:detail:{id}`, TTL 60s
- List cache: `cache:product:list:v{version}:...`, TTL 30s
- Event 기반 무효화: `ProductUpdatedEvent` → `@TransactionalEventListener(AFTER_COMMIT)`

### Reasoning
- **Cache-aside 패턴 선택**: Direct key + versioned key 조합은 Category/Brand/Search에서도 재사용 가능
- **Spring Event 기반 무효화**: PRD §96 준수, decoupling, transaction-after-commit 보장
- **TTL 선택**: Detail 60s (단일 상품), List 30s (목록 - 더 빈번한 변경)

### Evidence
- **테스트**: `./gradlew test --tests ProductPublicApiIT` → 12 tests completed, 0 failed ✅
- **AC 검증**: AC1 (cache hit), AC2 (invalidation), AC3 (status filter), AC4 (fallback) 모두 통과
- **코드**: `src/main/java/com/olive/commerce/product/ProductPublicService.java` (cache-aside 구현)
- **테스트**: `src/test/java/com/olive/commerce/product/ProductPublicApiIT.java` (Testcontainers + Redis)
